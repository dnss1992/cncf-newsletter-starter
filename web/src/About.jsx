import React from 'react';
import { Typography } from '@mui/material';

function About() {
  return (
    <Typography variant="body1">
      This project aggregates CNCF project updates and displays them using a React +
      Material UI interface.
    </Typography>
  );
}

export default About;
